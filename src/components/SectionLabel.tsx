/** The uppercase mono eyebrow that sits above every section heading. */
export function SectionLabel({ children }: { children: React.ReactNode }) {
  return <span className="brl-mono-label text-accent">{children}</span>
}
